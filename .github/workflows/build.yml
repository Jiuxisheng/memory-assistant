name: Build APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.9'
        channel: 'stable'
    
    - name: Create local.properties
      run: |
        echo "flutter.sdk=${{ env.FLUTTER_ROOT }}" > android/local.properties
        echo "flutter.versionCode=1" >> android/local.properties
        echo "flutter.versionName=1.0.0" >> android/local.properties
    
    - name: Get dependencies
      run: flutter pub get
    
    - name: Build APK (verbose)
      run: |
        flutter build apk --release --no-tree-shake-icons --verbose 2>&1 | tail -100
        echo "=== Checking build outputs ==="
        find build -name "*.apk" 2>/dev/null || echo "No APK found in build directory"
        ls -la build/app/outputs/flutter-apk/ 2>/dev/null || echo "flutter-apk directory not found or empty"
    
    - name: Find and copy APK
      run: |
        echo "Searching for APK files..."
        find . -name "*.apk" -type f 2>/dev/null
        # Create output directory and copy APK
        mkdir -p build/app/outputs/flutter-apk
        cp ./android/app/build/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/ 2>/dev/null || \
        cp ./android/app/build/outputs/apk/release/app-release-unsigned.apk build/app/outputs/flutter-apk/app-release.apk 2>/dev/null || \
        echo "APK copy failed"
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: release-apk
        path: build/app/outputs/flutter-apk/app-release.apk
    
    - name: Upload to Release
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        files: build/app/outputs/flutter-apk/app-release.apk
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
